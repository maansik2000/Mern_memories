import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  ul: {
    justifyContent: "space-around",
    alignContent: "center",
    width: "50%",
    padding: 20,
    margin: "10px auto",
  },

  
}));
